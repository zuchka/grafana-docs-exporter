name: Is Organization Member Example

on:
  issues:
    types: [opened, labeled]

jobs:
  is-member:
    name: Is org member?
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v4
      - name: Check if organization member
        id: is_organization_member
        uses: zuchka/community-actions@v1.1.4
        with:
          org: railwayapp
          username: 20k-ultra
          token: Bearer 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
      - name: Create Comment for external
        run: echo "${{ steps.is_organization_member.outputs.result}}"
